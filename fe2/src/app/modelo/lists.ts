export class Lists{
    constructor(
     public bolivianos: Number,
     public dolares: Number,
     public receipt: String, // varios recibos
     public assist: Boolean,
     public type: Number, //nuevo // nivelacion
     public person: string,
     public events: string,
     public modulars: string
    ){}
}